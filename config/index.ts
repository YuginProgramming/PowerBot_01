const express = {
    PORT: 3000
};

const crawler = {};

const pinger = {};

const models = [
    'channels',
    'logs',
    'pingers',
    'statuses'
];

export {
    express,
    crawler,
    pinger,
    models
};